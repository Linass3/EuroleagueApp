import SwiftUI
import CoreData

struct TeamDetailsView: View {
    
    var body: some View {
        NavigationView {
            Group {
                Text("Team Details")
                
            }
            .navigationTitle("Team")
            .navigationBarBackButtonHidden(true)
        }
    }
}
